/**
 * @zine/mobile - Expo React Native app
 *
 * This file exists for workspace compatibility.
 * The actual app entry point is configured by Expo Router in app/_layout.tsx
 *
 * Main entry point is: expo-router/entry (see package.json "main" field)
 */

export { ZINE_VERSION } from '@zine/shared';
