// apps/worker/src/trpc/routers/items.ts
// Placeholder - to be implemented by zine-8uh

import { router } from '../trpc';

/**
 * Items router - handles user item operations
 *
 * Procedures (to be implemented):
 * - inbox: Get items in the triage queue
 * - library: Get bookmarked items
 * - home: Get curated home sections
 * - get: Get a single item detail
 * - bookmark: Move an item to bookmarked
 * - archive: Move an item to archived
 * - updateProgress: Update consumption progress
 */
export const itemsRouter = router({});
