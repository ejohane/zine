// apps/worker/src/trpc/routers/sources.ts
// Placeholder - to be implemented by zine-gu3

import { router } from '../trpc';

/**
 * Sources router - handles user source/subscription operations
 *
 * Procedures (to be implemented):
 * - list: Get user's subscribed sources
 * - add: Subscribe to a new content source
 * - remove: Unsubscribe from a source (soft delete)
 */
export const sourcesRouter = router({});
